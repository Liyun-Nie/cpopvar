% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cpopvar_public_config.R
\name{cpopvar_config}
\alias{cpopvar_config}
\title{Build an in-memory cpopvar configuration}
\usage{
cpopvar_config(
  main_data,
  annotations = NULL,
  genome_regions = NULL,
  cds_lengths = NULL,
  region_info = NULL,
  group_info = NULL,
  species_order = NULL,
  gene_function_mapping = NULL,
  tasks = c("M01_distribution", "M03_hotspot"),
  task_options = list(),
  column_mappings = NULL,
  session_id = NULL,
  visualization_settings = NULL,
  output_settings = NULL,
  ...
)
}
\arguments{
\item{main_data}{Path to the main variant table.}

\item{annotations}{Optional annotation table path.}

\item{genome_regions}{Optional genome-region table path.}

\item{cds_lengths}{Optional CDS length table path.}

\item{region_info}{Optional region-info table path.}

\item{group_info}{Optional grouping table path.}

\item{species_order}{Optional species-order table path.}

\item{gene_function_mapping}{Optional gene-function mapping path.}

\item{tasks}{Character vector of task names to enable. Aliases such as
\code{M01_data_distribution} are accepted and mapped to the canonical YAML
names.}

\item{task_options}{Named list of per-task parameter lists. Names must be
canonical task names or aliases; values become \code{analysis_tasks[[task]]$parameters}.}

\item{column_mappings}{Optional nested column-mapping list. Auxiliary files
such as \code{group_info} and \code{species_order} must not be mapped.}

\item{session_id}{Optional session identifier.}

\item{visualization_settings}{Optional visualization settings list copied
into the config unchanged.}

\item{output_settings}{Optional output settings list copied into the config
unchanged.}

\item{...}{Must remain empty. Unknown arguments are rejected so module
parameters cannot be flattened onto this constructor.}
}
\value{
A \code{cpopvar_config} list that can be passed to
\code{\link[=validate_cpopvar_config]{validate_cpopvar_config()}} or \code{\link[=run_analysis]{run_analysis()}}.
}
\description{
Construct a YAML-isomorphic configuration list from stable workflow inputs.
The YAML file remains the serialized configuration contract; this helper
only builds the same nested structure in memory.
}
\details{
Core preprocessing, filtering, and normalisation tasks are enabled whenever
any visualisation task is requested. Module-specific options belong in
\code{task_options}, not in \code{run_analysis()}.
}
\examples{
\dontrun{
cfg <- cpopvar_config(
  main_data = "variants.csv",
  annotations = "annotations.csv",
  genome_regions = "genome_regions.csv",
  tasks = c("M01_distribution", "M03_hotspot")
)
result <- run_analysis(cfg, output_dir = tempdir())
}
}
