% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viz_utils.R
\name{save_plot}
\alias{save_plot}
\title{Unified vectorized plot saving function}
\usage{
save_plot(plot_object, base_path, config, width = NULL, height = NULL)
}
\arguments{
\item{plot_object}{Plot object (ggplot, pheatmap, or other)}

\item{base_path}{Character. File path without extension (e.g., "/path/to/plot")}

\item{config}{List. Global configuration object containing visualization settings}

\item{width}{Numeric. Override width in inches (optional)}

\item{height}{Numeric. Override height in inches (optional)}
}
\value{
Character. Path to saved file, or NULL if failed
}
\description{
This is the single entry point for all plot saving in the system.
It automatically detects plot type and saves as high-quality vector PDF.
}
\examples{
if (require("ggplot2")) {
  # Create a dummy plot and config for the example
  my_ggplot <- ggplot2::ggplot(data.frame(x = 1:10, y = 1:10), ggplot2::aes(x, y)) +
               ggplot2::geom_point()
  config <- list(visualization_settings = list(theme_and_sizing = list(
    default_width = 8, default_height = 6
  )))

  # Define a temporary path for the example output
  temp_plot_path <- tempfile(fileext = ".pdf")

  # Run the save_plot function
  save_plot(my_ggplot, sub("\\\\.pdf$", "", temp_plot_path), config)

  # Clean up the created file
  if (file.exists(temp_plot_path)) {
    print(paste("Example plot saved to:", temp_plot_path))
    unlink(temp_plot_path)
  }
}
}
