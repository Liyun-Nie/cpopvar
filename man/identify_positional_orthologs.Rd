% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/M04_helpers.R
\name{identify_positional_orthologs}
\alias{identify_positional_orthologs}
\title{Identify Position-based Orthologous IGS regions}
\usage{
identify_positional_orthologs(igs_info_df)
}
\arguments{
\item{igs_info_df}{Data frame containing IGS region information with columns:
\itemize{
\item species: Species identifier (character)
\item Region_Name: IGS region name in format "IGS_geneA-geneB" (character)
\item Type: Region type, should be "IGS" (character)
\item Other columns are preserved but not used
}}
}
\value{
Data frame with poiGS mapping containing columns:
\itemize{
\item species: Original species identifier
\item Region_Name: Original IGS region name
\item poiGS_ID: Standardized position-based orthologous IGS identifier
\item gene_pair: Standardized gene pair (sorted alphabetically)
\item original_gene_pair: Original gene pair before sorting
}
}
\description{
Converts species-specific IGS regions into cross-species comparable
position-based orthologous IGS (poiGS) units by analyzing flanking gene pairs.
This function solves the fundamental problem that IGS sequences vary dramatically
across species and cannot be directly compared.
}
\details{
Identify Position-based Orthologous IGS (poiGS) regions

The poiGS approach works by:
\enumerate{
\item \strong{Extracting flanking genes}: Parse Region_Name (format: IGS_geneA-geneB)
to identify the genes flanking each IGS region
\item \strong{Standardizing gene pairs}: Sort gene names alphabetically to ensure
IGS_psbA-trnH and IGS_trnH-psbA are treated as the same orthologous unit
\item \strong{Generating poiGS IDs}: Create identifiers such as
\code{poiGS_geneA-geneB} for cross-species comparison
}

This transforms the analysis unit from species-specific IGS regions to
position-based orthologous units that can be meaningfully compared across species.
}
\examples{
\dontrun{
# Load IGS region information
igs_data <- read.csv("output_gene_intergenic_intron_pos_length.csv") \%>\%
  filter(Type == "IGS")

# Generate poiGS mapping
poigs_mapping <- identify_positional_orthologs(igs_data)

# Check results
head(poigs_mapping)
length(unique(poigs_mapping$poiGS_ID))  # Number of orthologous IGS units
}

}
