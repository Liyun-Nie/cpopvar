% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/M04_igs_hotspot_module.R
\name{run_m04_igs_hotspot}
\alias{run_m04_igs_hotspot}
\title{Run M04 IGS-specific hotspot heatmap analysis}
\usage{
run_m04_igs_hotspot(
  normalized_data,
  config,
  output_dir,
  task_name,
  task_params
)
}
\arguments{
\item{normalized_data}{Normalized frequency data frame with columns:
\itemize{
\item species: Species identifier (character)
\item region_type: Genomic region type (character, must include "IGS")
\item gene: Gene/region identifier (character)
\item frequency_per_kb: Normalized frequency values (numeric)
\item var_type: Variant type (character)
Additional columns are preserved
}}

\item{config}{Configuration object containing analysis parameters}

\item{output_dir}{Output directory for generated plots and data}

\item{task_name}{Task identifier for output organization}

\item{task_params}{Task-specific parameters including data_scoping configuration}
}
\value{
List containing analysis results:
\itemize{
\item success: Boolean indicating successful completion
\item plots_generated: Number of plots created (typically 1)
\item igs_regions_analyzed: Number of IGS regions included
\item species_count: Number of species analyzed
\item output_files: List of generated file paths
\item error_message: Error details if analysis failed
}
}
\description{
Generates a focused heatmap visualization of IGS (Intergenic Spacer)
variation frequencies across species. This module provides a streamlined,
IGS-specific alternative to the comprehensive M03 hotspot analysis.
}
\details{
M04 IGS Hotspot Analysis Module Orchestrator

The M04 module implements a focused IGS analysis pipeline:
\enumerate{
\item \strong{Strict IGS filtering}: Applies data_scoping to retain only region_type == "IGS"
\item \strong{Data reshaping}: Transforms data into species × IGS_region frequency matrix
\item \strong{Heatmap generation}: Calls generate_global_frequency_heatmap with IGS-specific title
\item \strong{Quality logging}: Records analysis statistics and file locations
}

This module maintains consistency with M03's heatmap styling while providing
a dedicated focus on intergenic spacer regions. It explicitly excludes all
gene-centric analyses (PCA, correlation, enrichment) to maintain functional clarity.
}
\examples{
\dontrun{
# Run M04 IGS analysis
result <- run_m04_igs_hotspot(
  normalized_data = frequency_data,
  config = analysis_config,
  output_dir = session_paths$plots,
  task_name = "M04_igs_standard",
  task_params = igs_task_params
)

if (result$success) {
  cat("IGS heatmap generated:", result$output_files$heatmap, "\n")
}
}

}
