% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_m04_igs_analysis.R
\name{run_m04_igs_analysis}
\alias{run_m04_igs_analysis}
\title{Run M04 poiGS-based IGS hotspot discovery and analysis}
\usage{
run_m04_igs_analysis(
  normalized_data,
  config,
  output_dir,
  task_name,
  task_params
)
}
\arguments{
\item{normalized_data}{Normalized frequency data frame with columns:
\itemize{
\item species: Species identifier (character)
\item region_type: Genomic region type (character, must include "IGS")
\item gene: Gene/region identifier (character)
\item frequency_per_kb: Normalized frequency values (numeric)
\item var_type: Variant type (character)
Additional columns are preserved
}}

\item{config}{Configuration object containing analysis parameters}

\item{output_dir}{Output directory for generated plots and data}

\item{task_name}{Task identifier for output organization}

\item{task_params}{Task-specific parameters including data_scoping configuration}
}
\value{
List containing analysis results:
\itemize{
\item success: Boolean indicating successful completion
\item plots_generated: Number of plots created
\item poigs_regions_analyzed: Number of poiGS regions identified
\item core_hotspots_identified: Number of core shared hotspots
\item species_count: Number of species analyzed
\item output_files: List of generated file paths
\item error_message: Error details if analysis failed
}
}
\description{
Implements an advanced IGS hotspot discovery engine that solves the
fundamental problem of IGS sequence incomparability across species. Uses position-based
orthologous IGS (poiGS) approach to identify evolutionarily conserved high-variation
IGS regions with strong biological significance.
}
\details{
M04 Position-based Orthologous IGS Hotspot Analysis Engine

The M04 engine implements a sophisticated 4-step poiGS analysis pipeline:

\strong{Step 1: Position-based Orthologous IGS (poiGS) Identification}
\itemize{
\item Loads IGS region position information from region_length_statistics
\item Uses flanking gene pairs to define cross-species orthologous IGS units
\item Generates standardized poiGS identifiers (e.g., "poiGS_psbA-trnH")
\item Creates comprehensive poiGS mapping table
}

\strong{Step 2: Frequency Data Aggregation by poiGS}
\itemize{
\item Links variant frequency data with poiGS mapping
\item Aggregates frequencies by species and poiGS (handles multiple IGS per poiGS)
\item Produces cross-species comparable frequency dataset
}

\strong{Step 3: Two-tier Hotspot Filtering}
\itemize{
\item \strong{Candidate filtering}: Identifies high-frequency poiGS using M03 logic
\item \strong{Core hotspot filtering}: Retains poiGS shared across multiple species
\item Applies configurable thresholds for biological significance
}

\strong{Step 4: Information-dense Visualization}
\itemize{
\item Generates focused heatmap containing only core shared hotspots
\item Uses optimized visualization settings for maximum information density
\item Produces publication-ready output with biological annotations
}

Key innovations over basic M04:
\itemize{
\item Solves IGS cross-species comparability through poiGS approach
\item Implements rigorous hotspot filtering for biological significance
\item Generates information-dense rather than sparse visualizations
\item Provides comprehensive data provenance and quality metrics
}
}
\examples{
\dontrun{
# Run advanced M04 poiGS analysis
result <- run_m04_igs_analysis(
  normalized_data = frequency_data,
  config = analysis_config,
  output_dir = session_paths$plots,
  task_name = "M04_poigs_hotspot_discovery",
  task_params = igs_task_params
)

if (result$success) {
  cat("Core IGS hotspots identified:", result$core_hotspots_identified, "\n")
  cat("Final heatmap:", result$output_files$core_heatmap, "\n")
}
}

}
