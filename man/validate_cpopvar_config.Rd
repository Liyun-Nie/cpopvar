% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cpopvar_public_config.R
\name{validate_cpopvar_config}
\alias{validate_cpopvar_config}
\title{Validate a cpopvar configuration}
\usage{
validate_cpopvar_config(config, error = FALSE)
}
\arguments{
\item{config}{A YAML file path or an in-memory configuration list.}

\item{error}{If \code{TRUE}, invalid configurations raise an error instead of
returning the structured result.}
}
\value{
A list with \code{valid}, \code{errors}, \code{warnings}, and \code{config}. Each error
is a list with \code{code}, \code{path}, and \code{message}. \code{config} is a
\code{cpopvar_config} object when validation succeeds, otherwise \code{NULL}.
}
\description{
Validate a YAML path or in-memory configuration without writing a temporary
file. File-path and memory inputs use the same structure contract as the
parser and dispatcher.
}
\examples{
\dontrun{
result <- validate_cpopvar_config(cfg)
result$valid
}
}
